# R2 — object storage for images, files and backups

Generated from Cloudflare's OpenAPI schema — do not hand-edit.
Regenerate: `python3 tools/gen-cloudflare-refs.py --fetch`

Paths are relative to `https://api.cloudflare.com/client/v4`, with
`/accounts/{account_id}` or `/zones/{zone_id}` omitted for brevity —
prefix them back on. Auth is `Authorization: Bearer $USR_CLOUDFLARE_API_TOKEN`.

30 endpoints.

| Method | Path | What it does | Required |
|---|---|---|---|
| GET | `/r2/buckets` | List Buckets | — |
| POST | `/r2/buckets` | Create Bucket | name |
| DELETE | `/r2/buckets/{bucket_name}` | Delete Bucket | bucket_name |
| GET | `/r2/buckets/{bucket_name}` | Get Bucket | bucket_name |
| PATCH | `/r2/buckets/{bucket_name}` | Patch Bucket | bucket_name |
| PUT | `/r2/buckets/{bucket_name}` | Create Bucket (by name) | bucket_name |
| DELETE | `/r2/buckets/{bucket_name}/cors` | Delete Bucket CORS Policy | bucket_name |
| GET | `/r2/buckets/{bucket_name}/cors` | Get Bucket CORS Policy | bucket_name |
| PUT | `/r2/buckets/{bucket_name}/cors` | Put Bucket CORS Policy | bucket_name |
| GET | `/r2/buckets/{bucket_name}/domains/custom` | List Custom Domains of Bucket | bucket_name |
| POST | `/r2/buckets/{bucket_name}/domains/custom` | Attach Custom Domain To Bucket | bucket_name |
| DELETE | `/r2/buckets/{bucket_name}/domains/custom/{domain}` | Remove Custom Domain From Bucket | bucket_name, domain |
| GET | `/r2/buckets/{bucket_name}/domains/custom/{domain}` | Get Custom Domain Settings | bucket_name, domain |
| PUT | `/r2/buckets/{bucket_name}/domains/custom/{domain}` | Configure Custom Domain Settings | bucket_name, domain |
| GET | `/r2/buckets/{bucket_name}/domains/managed` | Get r2.dev Domain of Bucket | bucket_name |
| PUT | `/r2/buckets/{bucket_name}/domains/managed` | Update r2.dev Domain of Bucket | bucket_name |
| GET | `/r2/buckets/{bucket_name}/jobs` | List Bucket Jobs | bucket_name |
| GET | `/r2/buckets/{bucket_name}/jobs/{job_id}` | Get Bucket Job | bucket_name, job_id |
| GET | `/r2/buckets/{bucket_name}/lifecycle` | Get Object Lifecycle Rules | bucket_name |
| PUT | `/r2/buckets/{bucket_name}/lifecycle` | Put Object Lifecycle Rules | bucket_name |
| GET | `/r2/buckets/{bucket_name}/local-uploads` | Get Local Uploads Configuration | bucket_name |
| PUT | `/r2/buckets/{bucket_name}/local-uploads` | Put Local Uploads Configuration | bucket_name, enabled |
| GET | `/r2/buckets/{bucket_name}/lock` | Get Bucket Lock Rules | bucket_name |
| PUT | `/r2/buckets/{bucket_name}/lock` | Put Bucket Lock Rules | bucket_name |
| DELETE | `/r2/buckets/{bucket_name}/objects` | Delete Objects or Empty a Bucket | bucket_name |
| GET | `/r2/buckets/{bucket_name}/objects` | List Objects | bucket_name |
| DELETE | `/r2/buckets/{bucket_name}/objects/{object_key}` | Delete Object | bucket_name, object_key |
| GET | `/r2/buckets/{bucket_name}/objects/{object_key}` | Get Object | bucket_name, object_key |
| PUT | `/r2/buckets/{bucket_name}/objects/{object_key}` | Upload Object | bucket_name, object_key |
| POST | `/r2/temp-access-credentials` | Create Temporary Access Credentials | — |
